#!/bin/bash

# Install cURL library
sudo apt-get install libcurl4-openssl-dev

# Install cJSON library
# Assuming cJSON is installed using a package manager, adjust this based on your setup
# For example, on Ubuntu, you can install cJSON using:
sudo apt-get install libcjson-dev

# Install libharu (HPDF) library
# Assuming libharu is installed using a package manager, adjust this based on your setup
# For example, on Ubuntu, you can install libharu using:
sudo apt-get install libhpdf-dev
